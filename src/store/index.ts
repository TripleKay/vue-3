import { createStore } from 'vuex'

export default createStore({
  state: { //data
    name: 'TRIPLE-K',
  },
  getters: { //computed
  },
  mutations: { // action->use->change state data
  },
  actions: { //methods
  },
  modules: {
  }
})
